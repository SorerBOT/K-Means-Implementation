# K-Means-Implementation
The K-Means algorithm takes a set of points and an amount of "clusters" noted $k$, and returns $k$ "clusters" of points, that minimise the cumulative distance between all points in the same cluster.
